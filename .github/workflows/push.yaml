name: Evento Push
on:
    push:
        branches: ["main", "feature/*"]
        # branches-ignore:
        # path-ignore:
        paths:
            - "src/**"
        # tags:
        #     - v*

jobs:
    teste:
        runs-on: ubuntu-latest
        steps:
            
            - name: Action Teste
              run: echo "Action executada em main e feature "
